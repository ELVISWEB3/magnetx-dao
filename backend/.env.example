# Environment
# Use your Supabase pooled connection (pgbouncer, usually port 6543). Do not paste secrets here if you plan to commit.
# Include these recommended parameters for Prisma over pgbouncer.
# Example (replace with your values):
# DATABASE_URL=postgresql://{{USER}}:{{PASSWORD}}@aws-1-us-east-1.pooler.supabase.com:6543/{{DB}}?pgbouncer=true&connection_limit=1&sslmode=require
DATABASE_URL=postgresql://postgres.zidwlbcxocdeubfpoghp:IwO4IfwtS8FSnGu1
@aws-1-us-east-1.pooler.supabase.com:6543/postgres

# Direct, non-pooled connection (usually port 5432). Needed for Prisma Migrate only.
# Get it from Supabase Project Settings > Database > Connection string (non-pooled)
# DIRECT_DATABASE_URL=postgresql://{{USER}}:{{PASSWORD}}@{{HOST}}:5432/{{DB}}?sslmode=require
DIRECT_DATABASE_URL=postgresql://postgres:IwO4IfwtS8FSnGu1
@db.zidwlbcxocdeubfpoghp.supabase.co:5432/postgres

# Comma-separated production origins (no localhost). Example:
# CORS_ORIGIN=https://magnetxdao.com,https://www.magnetxdao.com
CORS_ORIGIN=https://magnetx-dao.vercel.app/

# Optional: Twitter/X API Bearer token to enrich follower count & avatar
TWITTER_BEARER_TOKEN=

# Server port (not used on Vercel, but used when running locally)
PORT=3001
