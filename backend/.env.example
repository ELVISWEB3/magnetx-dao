PORT=3001
# Relative to backend/ folder (used for local dev with SQLite)
DATABASE_FILE=./data.sqlite

# If set, the backend will use Postgres instead of SQLite (Render Managed PostgreSQL)
# Example: postgres://user:pass@host:5432/dbname
DATABASE_URL=

# Comma-separated origins for CORS (use * for any during dev)
CORS_ORIGIN=http://localhost:5173,http://127.0.0.1:5173,http://localhost:3000
